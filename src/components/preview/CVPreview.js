import PName from "./PName"

export default function CVPreview(props) {
    return (
        <section className="cvpreview-container">
            cv preview
            <PName />
        </section>
    )
}
