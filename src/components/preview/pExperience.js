export default function PExperience({ position, company, from, to }) {
    return (
        <>
            <h2>{position}</h2>
            <h4>{company}</h4>
            <h5>{from}</h5>
            <h5>{to}</h5>
        </>
    )
}
